SUBDIRS (1.0 2.0)

########################################################
# Install

#INSTALL(CODE "MESSAGE(\"Installing shared data...\")")
#INSTALL(FILES server.conf DESTINATION ${OM_CONFIG_DIR})
#INSTALL(FILES ctables/byte.xml DESTINATION ${OM_CONFIG_DIR}/ctables)
#INSTALL(PROGRAMS scheduler.sh DESTINATION ${OM_CONFIG_DIR})
#INSTALL(PROGRAMS synclayers.sh DESTINATION ${OM_CONFIG_DIR})
#INSTALL(PROGRAMS syncom.sh DESTINATION ${OM_CONFIG_DIR})
